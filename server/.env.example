# Скопируй этот файл в server/.env и впиши свой ключ Anthropic.
# Файл .env в git не попадает (см. .gitignore) — ключ останется только у тебя.

ANTHROPIC_API_KEY=sk-ant-впиши-сюда-свой-ключ

# Необязательно: порт AI-сервера (по умолчанию 3000)
# PORT=3000
